version: "3.8"

services:
  backend:
    build: ./backend
    container_name: dashboard_backend
    ports:
      - "5000:5000"
    env_file:
      - .env.example

  frontend:
    build: ./frontend
    container_name: dashboard_frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
